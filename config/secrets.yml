# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: e39f14530ce6273f96cb54125fffaa9f059ddd15a8a98753c6c9b02103b6d4fcaab0985ed4908492c17dd8dc000e64b062bcbeafe4a446975a11b020e48ecf02
  aws_access_key_id: <%= ENV["S3_KEY"] %>
  aws_secret_access_key: <%= ENV["S3_SECRET"] %>
  omniauth_provider_key: <%= ENV["OMNIAUTH_TWITTER_KEY"] %>
  omniauth_provider_secret: <%= ENV["OMNIAUTH_TWITTER_SECRET"] %>

test:
  secret_key_base: f09dedb1737cee5850a05dbc367775a2351a540e20b9aa6675f590d7be7eed0b86f216f954392a98ee90b0acec0525396b26204e03598a4ad255b9ae873b3ac8
  aws_access_key_id: <%= ENV["S3_KEY"] %>
  aws_secret_access_key: <%= ENV["S3_SECRET"] %>

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
  aws_access_key_id: <%= ENV["S3_KEY"] %>
  aws_secret_access_key: <%= ENV["S3_SECRET"] %>
  omniauth_provider_key: <%= ENV["OMNIAUTH_TWITTER_KEY"] %>
  omniauth_provider_secret: <%= ENV["OMNIAUTH_TWITTER_SECRET"] %>